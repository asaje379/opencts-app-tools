/*
 * By J. Salem AFFA
 * 
 * This module export initDatabase() function as default
 * 
 * initDatabase() allows you to create a MongoDB connection 
 *      with the configuration in 'settings.js' file.
 */

import mongoose from mongoose;
import config from './settings';

function initDatabase() {
    
}

export default initDatabase;